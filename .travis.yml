language: cpp
matrix:
  include:
    - os: osx
      osx_image: xcode10.2
      addons:
          homebrew:
            packages:
            - python
            - zeromq
            - msgpack
            - cmake
            - boost
            update: true

before_install:
    - eval "${MATRIX_EVAL}"

script:
    - ls

after_success:
    - echo "yeah!!"

# notifications:
#   email: false
#   irc:
#     channels:
#     - chat.freenode.net#rubinius
#     template:
#     - "%{repository}/%{branch} (%{commit} - %{author}): %{build_url}: %{message}"
